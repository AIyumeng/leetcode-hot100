# 48. rotate-image

## 🔗 题目链接
[LeetCode 48. Rotate Image](https://leetcode.com/problems/rotate-image/)

## 🧠 题目描述
给定一个 n × n 的二维矩阵 `matrix` 表示一个图像。请你将图像顺时针旋转 90 度。

**要求**：必须在**原地**旋转图像，这意味着你需要直接修改输入的二维矩阵。**不能**使用另一个矩阵来旋转图像。

**示例**：
- 示例 1：输入：matrix = [[1,2,3],[4,5,6],[7,8,9]]，输出：[[7,4,1],[8,5,2],[9,6,3]]；
- 示例 2：输入：matrix = [[5,1,9,11],[2,4,8,10],[13,3,6,7],[15,14,12,16]]，输出：[[15,13,2,5],[14,3,4,1],[12,6,8,9],[16,7,10,11]]。

---

## 💡 解题思路
核心思路：
- 转置+反转：将矩阵顺时针旋转90°可拆解为「矩阵转置」+「每行反转」两步操作，无需复杂的坐标计算；
- 原地操作：转置和反转均在原矩阵上完成，满足题目「原地修改」的要求；
- 对称交换：转置通过交换矩阵的对称元素（matrix[i][j] ↔ matrix[j][i]）实现，反转通过交换行内对称位置元素实现。

步骤：
1. 矩阵转置：遍历矩阵上三角区域（i < j），交换 matrix[i][j] 和 matrix[j][i]；
2. 每行反转：遍历矩阵的每一行，将行内元素左右反转（如 [1,2,3] → [3,2,1]）；
3. 完成两步操作后，矩阵即为顺时针旋转90°的结果。

---

## 🧩 代码
[code](../solutions/48.rotate-image.py)


---

### 总结
1. 核心技巧：将旋转拆解为「转置+反转」，把复杂的坐标映射问题转化为两步简单操作，降低思维难度；
2. 原地操作关键：转置时仅遍历上三角区域（i<j）避免重复交换，反转行用切片 `[::-1]` 实现原地修改；
3. 时间复杂度：O(n²)（遍历矩阵两次），空间复杂度：O(1)（仅使用常数级临时变量），符合题目要求。